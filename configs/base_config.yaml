paths:
  pdf_dir: data/pdf
  text_dir: data/text
  super_chunk_dir: data/chunks/super
  fine_chunk_dir: data/chunks/fine
  super_embedding_dir: data/embeddings/super
  fine_embedding_dir: data/embeddings/fine
  results_dir: results

embedding:
  model_name: sentence-transformers/all-MiniLM-L6-v2
  batch_size: 16

retrieval:
  top_k_super: 5
  top_k_fine: 10
  hybrid_alpha: 0.3

classifier:
  model_name_or_path: distilbert-base-uncased

